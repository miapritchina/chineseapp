    out.set(`recognition|meaningRecognition|${key}`, {
      itemKind: "recognition",
    out.set(`recognition|soundRecognition|${key}`, {
      itemKind: "recognition",
test("single saved recognition seeds a recognition card", () => {
  assert.ok(m.has("recognition|recognition|请"));
  assert.ok(m.has("recognition|recognition|好"));
test("multi-char recognition seeds phoneticTap on each char with a sound component", () => {
  assert.ok(m.has("recognition|recognition|你好"));
test("two saved recognitions sharing a char dedupe to one phoneticTap card", () => {
  // Map size = 2 recognition-recognition + 1 phoneticTap
  assert.ok(m.has("recognition|recognition|?"));
